services:
  koda-pipeline:
    build: .
    ports:
      - "8765:8765"
    environment:
      KODA_SERVER_HOST: "0.0.0.0"
      KODA_SERVER_PORT: "8765"
    volumes:
      - ./koda_pipeline_downloads:/app/koda_pipeline_downloads
